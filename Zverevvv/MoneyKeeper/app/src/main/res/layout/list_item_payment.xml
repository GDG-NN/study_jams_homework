<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content">

    <TextView
        android:id="@+id/list_item_payment_sum"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentRight="true"
        android:padding="4dp"
        tools:text="Sum"/>

    <TextView
        android:id="@+id/list_item_payment_name"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_toLeftOf="@id/list_item_payment_sum"
        android:textStyle="bold"
        android:padding="4dp"
        tools:text="Name"/>

    <TextView
        android:id="@+id/list_item_payment_date"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_toLeftOf="@id/list_item_payment_sum"
        android:layout_below="@id/list_item_payment_name"
        android:padding="4dp"
        tools:text="Date"/>



</RelativeLayout>